# Novolis.Windows.Input

Win32 input injection primitives for a Windows interactive session.

The package accepts screen coordinates and virtual-key values. Product
protocols own their platform-independent input messages and translate them
before calling this package.

## Install

```xml
<PackageReference Include="Novolis.Windows.Input" Version="2026.1.*" />
```
